# 刷题学习 · Design Contract

本文件约束后续所有界面修改，避免项目重新走向“功能堆叠”和局部自由发挥。

## 产品边界

这是一个以刷题促进学习的轻量网页应用，不是课程平台、知识库门户、考试管理后台或数据大屏。

核心闭环：

`开始练习 → 作答 → 立即反馈 → 看解析 → 错题重练 → 逐步掌握`

新增功能必须直接服务这个闭环，否则默认不进入一级界面。

## 信息架构

一级入口固定为：

`首页 / 练习 / 错题 / 我的`

- 首页：继续学习、今日进度、快速练习、章节入口
- 练习：选择练习方式和章节
- 错题：重练与薄弱点
- 我的：统计和必要设置

不再新增“阅读模式 / 复习资料 / 知识体系”等职责相近的一级入口。题目解析和知识点直接依附于做题流程。

## 页面关系

每个视区优先一个主任务。

- 首页主任务：继续学习
- 练习页主任务：选择范围并开始
- 做题页主任务：理解题干并作答
- 错题页主任务：找到需要巩固的题并重练
- 我的主任务：查看记录或调整设置

辅助信息不得与主任务竞争最高视觉强调。

## 视觉语言

性格：**克制、清晰、紧凑、精确、安静**。

主要层级顺序：

`位置 → 空间 → 字重 → 字号 → 灰度 → 强调色 → 语义色 → Surface`

### 配色

- Canvas：暖中性色
- Surface：接近白色 / 深色模式对应深中性色
- Interface Accent：深青绿色系
- Success：绿色
- Danger：红色
- Warning：低饱和琥珀色

不通过五颜六色区分普通章节或功能。

### Surface

优先级：

`裸排 → 间距分组 → 背景变化 → 分割线 → 独立 Surface`

普通内容不自动加卡片、阴影、渐变。阴影只用于真正浮动的层级。

## 对齐与尺寸

- 文本与文本优先基线关系
- 重复列表共享起始边和结束边
- 同组按钮共享高度
- 数字统计使用稳定数字宽度
- 选中 / 正确 / 错误状态不得改变选项几何尺寸
- 点击区域和视觉尺寸可以分离，但点击区域不得过小
- 禁止使用随机 `translate`、负 margin 或临时 offset 修基础对齐

## 做题页规则

做题页必须保持最安静：

- 顶部只保留退出、当前练习名称、题号、进度
- 题型作为低权重标签
- 题干是最高内容焦点
- 选项统一行高、边界、字母圆标
- 单选 / 判断可直接判题；多选必须显式提交
- 作答后原位显示正确 / 错误状态，不跳动布局
- 解析紧跟题目，不做独立页面
- 底部保留上一题 / 下一题

## 错题规则

- 答错立即进入错题
- 错题连续答对两次后自动移出待复习
- 错题页先展示待复习数量，再展示薄弱知识点，再展示具体题目
- 不引入复杂算法术语给用户

## 响应式

桌面和手机保持同一任务顺序。

- 桌面：顶部导航
- 手机：底部四项导航
- 做题期间隐藏全局导航，避免干扰
- 窄屏改为纵向堆叠，不缩小正文到难读尺寸

## 图标与平台外壳

- 图标使用统一线性 SVG 风格
- 不用 Emoji 充当核心导航图标
- 不使用 macOS 三色点、Windows 标题栏等系统外壳
- 不照搬粉笔、考试宝、驾考宝典或其他具体产品的视觉组件
- 可以借鉴成熟产品的任务逻辑，但必须使用本项目自己的视觉表达

## 变更检查

每次 UI 修改至少检查：

1. 是否增加了不必要的一级入口
2. 当前页面主任务是否仍清楚
3. 对齐与间距是否来自既有节奏
4. 是否出现多余 Card / Border / Shadow
5. Accent 是否被滥用
6. 状态切换是否导致几何跳动
7. 手机端是否保持任务顺序与可操作性
8. 是否仍可在无框架静态环境下运行
